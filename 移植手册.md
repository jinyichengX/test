# IPGUI 移植手册

## 1. 移植目录

```
├── includes/          # 全局配置 & 核心类型定义
│   ├── ipgui_conf.h       ★ 必须修改 — 配置项
│   ├── ipgui_types.h      ★ 必须检查 — 平台类型定义
│   ├── ipgui_utils.h          — 编译宏 & 工具宏（多编译器适配）
│   ├── ipgui_color.h          — 颜色类型 & 宏
│   ├── ipgui_core.h           — surface & 核心初始化
│   ├── ipgui_coord.h          — 坐标类型
│   ├── ipgui_defs.h           — 常量
│   ├── ipgui_angle.h          — 三角函数 LUT
│   ├── ipgui_graphic2.h       —
│   └── ipgui_lcd_pix_fmt.h    — 像素格式枚举
├── base/              # 基础库（仅绘图需要的子集如下）
│   ├── ipgui_memory.c/h    — 内存分配（★ 必须）
│   ├── ipgui_debug.c/h     — 调试打印 & assert（★ 必须）
│   ├── ipgui_math.c/h      — 数学函数（sin/cos，★ 必须）
│   ├── ipgui_avl.c/h       — AVL 树（多边形光栅化用，★ 必须）
│   ├── ipgui_membox.c/h    — 内存块池（多边形用，★ 必须）
│   ├── ipgui_mempool.c/h   — 伙伴内存池（mask/image buffer，★ 必须）
│   │
│   └── 以下文件绘图库不需要，可移除：
        ipgui_time.c/h      — 系统时基
        ipgui_queue.c/h     — 优先队列
        ipgui_norm_queue.c/h— 通用队列
        ipgui_darray.c/h    — 动态数组
        ipgui_vector.c/h    — 向量运算
├── composite/         # 混合（blend）引擎
├── gfx/               # 图形绘制（三角、线、弧、圆角矩形、圆等）
├── port/              ★ 用户实现 — 平台适配接口
│   └── ipgui_usr_putchar.c   — 重写 ipgui_putck() 对接串口
├── ipgui_core.c           — 库初始化入口

```

---

## 2. 第一步：修改 `includes/ipgui_conf.h`

### 2.1 必须改的项

| 宏 | 含义 | 建议值 |
|----|------|--------|
| `IPGUI_SMEM_SIZE` | 内核堆大小（字节） | 裸机≥128KB，带图形≥256KB |
| `IPGUI_ENDIAN_LITTLE` | 字节序：1=小端，0=大端 | MCU 通常为 1 |

### 2.2 可修改的项

| 宏 | 含义 | 默认值 | 说明 |
|----|------|--------|------|
| `IPGUI_GRADIENT_LUT_EN` | 渐变 LUT 加速 | `0` | 开启需 64KB 内存 |
| `IPGUI_GRADIENT_STOP_MAX` | 渐变最大颜色数 | `5` | 必须 ≥2 |
| `USE_INV_TABLE` | 除法 LUT | `1` | 512B，加速渐变和 32 位像素混合 |
| `CORNER_CACHE_ITEM_MAX_NUM` | 角点缓存项数 | `10` | 绘制圆角矩形时使用 |

## 3. 第二步：检查 `includes/ipgui_types.h`

所有 typedef 必须逐项核对目标平台的字宽。

### 常见平台修改示例

**32 位 ARM CPU：**

```c
typedef unsigned int   u32_t;
typedef unsigned short u16_t;
typedef unsigned char  u8_t;
typedef int            s32_t;
typedef short          s16_t;
typedef signed char    s8_t;
typedef long long      s64_t;
typedef unsigned long long u64_t;
typedef unsigned int   uintptr_t;
typedef unsigned int   ipgui_tick_t;
```

**64 位系统（当前默认值）：**

```c
// ... 同上 ...
typedef unsigned long long uintptr_t;
```

---

## 4. 第三步：实现平台适配 `port/`

### 4.1 串口打印 — `ipgui_putck()`

库中的 `base/ipgui_debug.c` 声明为 `__WEAK__` 弱符号。用户只需在自己的源文件中定义同名强符号覆盖它：

```c
// port/ipgui_usr_putchar.c
void ipgui_putck(char c)
{
    // 替换为自己的串口发送函数，例如：
    // USART_SendData(USART1, c);
    // while (!(USART1->SR & USART_SR_TXE));
}
```

无需修改库代码，编译器自动链接强符号版本。

## 5. 第四步：初始化

```c
#include "ipgui_core.h"
#include "ipgui_color.h"

int main(void)
{
    ipgui_err_t err = ipgui_init();
    if (err != IPGUI_ERR_OK) {
        // 初始化失败（通常内存不足）
        while (1);
    }
    // ... 绘图 ...
}
```

---

## 6. 绘图

> 完整 API 参考见 **`API参考手册.md`**，包含每个函数的参数表、结构体字段说明和代码示例。

## 7. 重要注意事项

### 7.1 编译器支持
`ipgui_utils.h` 自动适配：GCC / Clang / IAR / ARMCC v5 & v6 / MSVC。属性宏（`__WEAK__`, `__PACKED__`, `IPGUI_ST_ALIGN` 等）在各平台自动展开为对应语法。