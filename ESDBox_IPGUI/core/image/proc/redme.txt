1. ipgui_image_geometry_transform需要修改ipgui_image_transform,包括像素格式转换，定点数运算导致的精度损失从而引起图像边角显示不全（映射到原图的位置不准）
    （完成）
2.  重要优化（完成）            
        xo = inv.a * x + inv.b * y;
            yo = inv.c * x + inv.d * y;
            这两个的计算可以使用增量的方式，而不是每次都这样计算（看不懂这句话 问AI）
3. 需要继续修改的地方1
    446和542行，写入pixmap要批量写入（假设ARGB8888）*(unsigned int*)(pixmap + idx * px_sz) = (cr[3] << 24) | (cr[0] << 16) | (cr[1] << 8) | cr[2];  // ARGB
    439行，计算ARGB值要根据不同的像素格式来修改，不能像那样写死4通道
4. 需要继续修改的地方2
    做完变换后的图像在内存中等待使用，需要写一个图像内存管理
5. 需要继续修改的地方3
    插值策略参数ipgui_lerp_method_t没有用到