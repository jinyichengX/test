#include "ipgui_widget_temp.h"

/* 将部件部分区域标记为脏 */
void ipgui_widget_part_mark_dirty(void)
{

}


void ipgui_widget_rel_move(ipgui_coord_t x, ipgui_coord_t y)
{
    //     // 1. 标记旧位置为脏
    // ipgui_widget_mark_dirty(w);
    // // 2. 更新坐标
    // w->x = new_x; w->y = new_y;
    // w->absolute_dirty = true;
    // // 3. 标记新位置为脏
    // ipgui_widget_mark_dirty(w);
}