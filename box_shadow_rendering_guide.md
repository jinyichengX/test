# 盒子阴影（Box Shadow）渲染原理手册

---

## 1. 概述

盒子阴影（`box-shadow`）的绘制目标：给定一个带圆角的矩形区域（可能有四个各不相同的圆角半径），在该区域的**外侧**或**内侧**生成一个带模糊效果的阴影。

核心参数：

| 参数 | 含义 |
|------|------|
| `offset_x` | 阴影水平偏移 |
| `offset_y` | 阴影垂直偏移 |
| `blur` | 模糊半径（标准差） |
| `spread` | 扩展半径，正值向外扩展，负值向内收缩 |
| `color` | 阴影颜色（含 alpha） |
| `inset` | `true` = 内阴影，`false` = 外阴影 |

---

## 2. 整体绘制管线

```
[原始圆角矩形]
    │
    ▼ 应用 spread
[扩展后的阴影形状 S]  ← 这个形状本身也是圆角矩形，但半径被 spread 修正
    │
    ▼ 应用 blur（高斯模糊）
[模糊后的阴影图像]  ← S 与 高斯核做卷积
    │
    ▼ inset 处理
    ├─ 外阴影：直接绘制在矩形后方
    └─ 内阴影：用矩形裁剪，只保留矩形内部部分，且仅保留 S 的"内侧"区域
```

**关键点**：spread 发生在 blur 之前。顺序不能颠倒。

---

## 3. 阴影形状的生成（Spread 阶段）

### 3.1 外阴影（outset）

给定原始矩形 `(x, y, w, h)` 和四个圆角半径 `(rl, rt, rr, rb)`（分别对应左上、右上、右下、左下）：

```
扩展后的阴影形状 = 原始矩形 + spread 在所有四个方向上的均匀扩展

新矩形边界：
  x' = x - spread
  y' = y - spread
  w' = w + 2 × spread
  h' = h + 2 × spread
```

**圆角半径的修正公式**：

扩散后的圆角半径 `r'` 由原半径 `r` 和 spread 共同决定。CSS 规范定义了一个过渡公式（近似）：

```
如果 spread ≥ 0：
  r' = r + spread × factor
其中 factor 是一个修正系数，大约在 1.0 ~ 1.5 之间，
具体行为：当原半径 = 0 时，r' 略大于 spread（防止尖角变圆太突兀）；
当原半径 > 0 时，r' 向 r + spread 平滑过渡。
```

更精确地，参考 W3C 规范，实际使用的公式是：
```
r' = max(0, r + spread × (1 + (1 - (r / (r + spread))) × k))
```
简化实践中常用：
```
如果 r == 0：
  r' = spread × adjustment  （adjustment ≈ 一个很小的正数，让扩散后依然接近直角）
如果 r > 0：
  r' = r + spread
```

**四个角的半径独立修正**，互不影响——这是支持四个不同圆角半径的关键。

### 3.2 内阴影（inset）

内阴影的 spread 方向相反：

```
收缩后的阴影形状：
  x' = x + spread
  y' = y + spread
  w' = w - 2 × spread
  h' = h - 2 × spread
```

半径修正方向也相应反转：
```
r' = max(0, r - spread)
```

**注意**：当 `spread` 为负值时，外阴影收缩、内阴影扩张，行为也相应反转。

### 3.3 负 spread 的处理

```
spread < 0 时：
  - 外阴影：矩形缩小，可能变为空（完全消失）
  - 内阴影：矩形扩大，可能超出原始矩形范围
  - 圆角半径：r' = max(0, r + spread) （外阴影）
               r' = max(0, r - spread) （内阴影，spread 为负时 r' 增大）
```

---

## 4. 模糊阶段（Blur）

### 4.1 高斯模糊的数学定义

2D 高斯核：

```
G(x, y) = (1 / (2πσ²)) × exp(-(x² + y²) / (2σ²))
```

其中：
- `σ`（sigma）是标准差，控制模糊程度
- `blur` 参数与 `σ` 的关系：**σ ≈ blur / 2**（经验法则，CSS 规范的实际实现约为 `σ = blur / 2`）

### 4.2 核半径（Kernel Radius）

在实践中，高斯核会被截断。通常取：

```
kernel_radius = ceil(3 × σ) = ceil(1.5 × blur)
```

即核的覆盖范围约为 `(2 × kernel_radius + 1) × (2 × kernel_radius + 1)` 像素。

### 4.3 衰减函数（Decay Function）

阴影从形状边缘向外（或向内）衰减的方式完全由高斯函数决定：

```
在距离边缘 d 处的透明度 ∝ exp(-d² / (2σ²))
```

**关键视觉效果**：
- `d = 0`（边缘处）：透明度 = 0%（完全不透明，原始颜色）
- `d = σ`：透明度衰减到约 60.7%（即 `1 - exp(-0.5)`）
- `d = 2σ`：透明度衰减到约 13.5%
- `d = 3σ`：透明度衰减到约 1.1%（几乎完全透明）

### 4.4 衰减方向

**外阴影**：从阴影形状边缘**向外**衰减。

```
[原始矩形]
███████████████████████████████  ← 完全不透明（阴影形状内部）
███████████████████████████████
████████░░░░░░▒▒▒▒▓▓▓▓▓▓██████  ← 向外衰减
██████░░░░▒▒▒▒▓▓▓▓▓▓▓▓▓▓██████
████░░▒▒▒▒▓▓▓▓▓▓▓▓▓▓▓▓▓▓██████  ← 越远越淡
```

**内阴影**：从阴影形状边缘**向内**衰减。

```
██████▓▓▓▓▒▒▒▒░░░░░░░░░░░░██████  ← 从边缘向内衰减
████████▓▓▓▓▒▒▒▒░░░░░░░░████████
██████████▓▓▓▓▒▒▒▒░░░░██████████  ← 中心完全透明（原始矩形内部）
```

### 4.5 分离卷积优化

2D 高斯核是**可分离的**，可以先对水平方向做 1D 卷积，再对垂直方向做 1D 卷积：

```
水平 pass：对每一行做 1D 高斯卷积
垂直 pass：对每一列做 1D 高斯卷积
```

时间复杂度从 `O(k² × w × h)` 降为 `O(2k × w × h)`。

### 4.6 1D 高斯核的生成

```c
// 伪代码
float sigma = blur / 2.0f;
int radius = (int)ceil(3.0f * sigma);
int kernel_size = 2 * radius + 1;

float sum = 0.0f;
for (int i = 0; i < kernel_size; i++) {
    float x = (float)(i - radius);
    kernel[i] = exp(-(x * x) / (2.0f * sigma * sigma));
    sum += kernel[i];
}
// 归一化
for (int i = 0; i < kernel_size; i++) {
    kernel[i] /= sum;
}
```

### 4.7 blur 参数对视觉的影响

| blur 值 | 视觉效果 |
|---------|---------|
| `blur = 0` | 无模糊，阴影形状边缘锐利 |
| `blur = 5` | 轻微柔和阴影，衰减距离约 7.5px |
| `blur = 10` | 明显模糊，衰减距离约 15px |
| `blur = 20` | 大幅模糊，衰减距离约 30px，阴影很"大" |
| `blur = 50` | 极度模糊，几乎只剩一个淡淡的色块 |

---

## 5. Spread 参数详解

### 5.1 视觉效果

| spread | 外阴影效果 | 内阴影效果 |
|--------|-----------|-----------|
| `spread = 0` | 阴影形状与原始矩形等大 | 阴影形状与原始矩形等大 |
| `spread > 0` | 阴影往外"膨胀"，阴影区域更大 | 阴影往内"收缩"，内部阴影区域变小 |
| `spread < 0` | 阴影往内"收缩"，阴影区域变小 | 阴影往外"膨胀"，内部阴影区域变大 |

### 5.2 spread 与 blur 的交互

```
spread 发生在 blur 之前：
  spread 改变阴影形状 → 然后对改变后的形状做 blur
```

这意味着：
- **spread > 0 + blur > 0**：阴影先膨胀再模糊，看起来阴影范围很大，边缘柔和
- **spread < 0 + blur > 0**：阴影先收缩再模糊，阴影贴近盒子，边缘柔和但范围缩小
- **spread > 0 + blur = 0**：纯形状扩展，边缘锐利（常用于模拟边框）
- **spread < 0 + blur = 0**：纯形状收缩，边缘锐利

---

## 6. 圆角半径的详细处理

### 6.1 四个独立圆角半径

盒子可以有四个不同的圆角半径：

```
border-radius: r_tl  r_tr  r_br  r_bl;
              ↑       ↑      ↑      ↑
           左上     右上   右下   左下
```

阴影形状的每个角也独立地根据对应的原角半径进行 spread 修正。

### 6.2 半径过大时的处理

如果某个圆角半径超过了矩形边长的一半，各半径需要等比例缩小，使得同一方向的半径之和不超过对应边长：

```
if (r_tl + r_tr > w) {
    float scale = w / (r_tl + r_tr);
    r_tl *= scale;
    r_tr *= scale;
}
if (r_bl + r_br > w) {
    float scale = w / (r_bl + r_br);
    r_bl *= scale;
    r_br *= scale;
}
if (r_tl + r_bl > h) {
    float scale = h / (r_tl + r_bl);
    r_tl *= scale;
    r_bl *= scale;
}
if (r_tr + r_br > h) {
    float scale = h / (r_tr + r_br);
    r_tr *= scale;
    r_br *= scale;
}
```

**这个比例缩小同样适用于阴影形状。**

### 6.3 圆角处阴影的视觉特征

圆角区域的阴影模糊是各向同性的（高斯核是圆对称的），但视觉上会感觉到：

- **凸角（外阴影）**：阴影在角部比直边看起来更"亮"（密度更大），因为角部同时接收来自两个方向的阴影扩散
- **凹角（内阴影）**：阴影在角部比直边更"暗"（衰减更快），因为角部被两侧同时裁剪

---

## 7. 内阴影与外阴影的区别

### 7.1 外阴影（outset）

```
绘制顺序：
1. 生成阴影形状（原始矩形 + spread 向外扩展）
2. 对整个阴影形状区域做高斯模糊
3. 将该模糊结果绘制在原始矩形"后方"（先绘制阴影，再绘制盒子）
4. 裁剪：只保留原始矩形"外部"的阴影（矩形内部不显示阴影）
```

裁剪方式：用原始矩形的**反向遮罩**——原始矩形区域为透明，外部为不透明。

### 7.2 内阴影（inset）

```
绘制顺序：
1. 生成阴影形状（原始矩形 - spread 向内收缩）
2. 对整个阴影形状区域做高斯模糊
3. 用原始矩形裁剪，**只保留矩形内部**的阴影
4. 裁剪：在阴影形状**内部**不显示（即阴影形状内部透明，形状到矩形边界之间显示衰减）
5. 内阴影绘制在盒子内容"上方"（盖在背景之上、内容之下）
```

内阴影的核心裁剪逻辑：

```
final_alpha = blur_result_alpha          // 高斯模糊的结果
            × inside_original_rect        // 在原始矩形内 = 1，外 = 0
            × outside_shadow_shape        // 在阴影形状外 = 1，内 = 0
```

即最终可见的阴影是：**原始矩形内部** 且 **阴影形状外部** 的区域。

这就是为什么内阴影看起来像是从边缘向内衰减——阴影形状内部（离边缘较远的地方）被裁剪掉了。

### 7.3 对比总结

| 特性 | 外阴影 | 内阴影 |
|------|--------|--------|
| spread 方向 | 向外 | 向内 |
| 模糊衰减方向 | 从边缘向外 | 从边缘向内 |
| 裁剪区域 | 原始矩形外部 | 原始矩形内部 |
| 阴影形状内部 | 完全不透明 | 完全透明（被裁剪） |
| 绘制层级 | 盒子下方 | 背景之上、内容之下 |
| 偏移方向感 | 偏移方向 = 光源方向 | 偏移方向 = 阴影方向（和直觉相反） |

---

## 8. 裁剪与合成

### 8.1 外阴影的遮罩生成

```
外阴影遮罩 = NOT(原始矩形区域)
即：原始矩形区域内像素 = 0（不显示阴影），外部 = 1（显示阴影）
```

### 8.2 内阴影的遮罩生成

```
内阴影遮罩 = 原始矩形区域 AND NOT(阴影形状区域)
即：原始矩形内部 且 阴影形状外部 = 显示阴影
    原始矩形外部 或 阴影形状内部 = 不显示阴影
```

### 8.3 偏移的影响

偏移（`offset_x`, `offset_y`）在**模糊之后**应用：

```
最终阴影位置 = 模糊结果 + (offset_x, offset_y)
```

偏移不应影响遮罩形状。遮罩始终以原始矩形为基准。

---

## 9. 实现架构建议

### 9.1 推荐流程

```
Step 1: 根据原始矩形参数和 spread，计算阴影形状的四个角半径和边界
Step 2: 判断阴影形状是否可见（面积 > 0）
Step 3: 为阴影分配临时缓冲区（尺寸 = 阴影边界 + blur 扩展）
Step 4: 在临时缓冲区中绘制阴影形状（填充为纯色 1.0）
Step 5: 对临时缓冲区做高斯模糊（两 pass 分离卷积）
Step 6: 生成最终遮罩（根据 inset/outset 裁剪）
Step 7: 将 color 乘以遮罩 alpha，合成到目标画布
```

### 9.2 边界计算

```
阴影缓冲区边界：
  shadow_x = x' - blur_padding   （x' 为 spread 后的 x）
  shadow_y = y' - blur_padding   （y' 为 spread 后的 y）
  shadow_w = w' + 2 × blur_padding
  shadow_h = h' + 2 × blur_padding

其中 blur_padding = ceil(3 × σ) = ceil(1.5 × blur)
```

### 9.3 关键注意事项

1. **spread 先于 blur**：不能先 blur 再 spread
2. **四个角半径独立计算**：每个角使用各自的原半径
3. **半径不能为负**：spread 修正后 `r' = max(0, r + spread)`（外阴影）
4. **高斯核需截断并归一化**：避免能量损失
5. **内阴影的双重裁剪**：原始矩形裁剪 + 阴影形状反转裁剪
6. **边缘处理**：缓冲区边缘需扩展 blur_padding 避免截断

---

## 10. 特殊场景

### 10.1 blur = 0, spread = 0

最简单的阴影——就是原始矩形的平移拷贝，无模糊无扩展。

### 10.2 spread 为负且绝对值很大

阴影形状可能完全消失（面积为 0），此时不应绘制任何阴影。

### 10.3 圆角半径为 0（直角矩形）

阴影形状的角也是直角。模糊后直角会自然变"圆"——这是高斯模糊的自然效果，不需要特殊处理。

### 10.4 一个或多个圆角半径极大

按照 6.2 节的比例缩小规则处理。缩小时四个半径需要保持比例关系。

### 10.5 多个 box-shadow 叠加

CSS 支持多个 `box-shadow`（逗号分隔）。渲染时按声明顺序从后往前绘制（列表第一个在最上面），每个阴影独立走完整管线。

---

## 11. 参考

- W3C CSS Backgrounds and Borders Module Level 3 — [box-shadow 规范](https://www.w3.org/TR/css-backgrounds-3/#box-shadow)
- 高斯模糊可分离性：任何图像处理教材均有说明
- 圆角过渡公式：参考 W3C 中 `border-radius` 的 spread 过渡规则
